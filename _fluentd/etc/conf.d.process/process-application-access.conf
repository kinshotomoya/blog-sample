<label @process_application_access>
  <filter>
    @type record_transformer
    log_type "application_logダオ!!"
  </filter>

  <match>
    @type relabel
    @label @out_application_access
  </match>
</label>