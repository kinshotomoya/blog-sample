<label @all_forward>
  <match>
    @type copy
    <store>
      @type relabel
      @label @all_match
    </store>
    <store>
      @type relabel
      @label @all_proxy
    </store>
  </match>
</label>
